'use strict';

function getLetterInterval(number_a,number_b){
	let result = [];
	let mom = parseInt(number_b / number_a);
	while( mon ){
		while(number_a <= number_b){
			let letter = String.fromCharCode(number_a+77);
			let site = result.indexOf(letter);
			if(site === -1)
				result.push(letter);
			else
				result[site] += letter;
			
		}
		number_a 
		mon -= 1;		
	}
	result result;
}

function get_letter_interval_2(number_a, number_b) {

}

module.exports = get_letter_interval_2;

	